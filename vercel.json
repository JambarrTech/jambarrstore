{
  "installCommand": "npm install && cd next && npx prisma generate && cp -r ../node_modules/@prisma node_modules/ && cp -r ../node_modules/.prisma node_modules/ 2>/dev/null; true",
  "buildCommand": "cd next && npm run build",
  "outputDirectory": "next/.next",
  "framework": "nextjs",
  "regions": ["cdg1"]
}